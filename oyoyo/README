oyoyo irc lib 
-------------

a small, simple irc lib for python suitable for bots, clients and anything 
else you can think of ( even games )

    * clean and simple design 
    * multiserver 
    * multiroom 
    * autoreconnect
    * low level bot framework ( see oyoyo_bot package for a full bot )
    * timed callbacks
    * and not a thread in sight! 

Install
-------

the usual

    $ python setup.py install

or for developers

    $ python setup.py develop --install-dir=/some/path
    
